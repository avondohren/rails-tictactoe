<h1><%= @user.name %></h1>

<p><%= session[:user_id] %> --- <%= params[:id] %></p>

<% if session[:user_id] == params[:id] %>
  <p>
    <%= link_to("Edit User Info", edit_user_path(@user.id)) %>
  </p>
<% end %>