<h1>Games</h1>

<% @games.each do |game| %>
  <p>
    <%= game.id %> <%= link_to(game.user.name, game_path(game.id)) %>
  </p>
<% end %>
<p>
  <%= link_to("New Game", games_path(current_user.id), :method=> :post) %>
</p>